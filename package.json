{
  "name": "finflux-app",
  "version": "1.0.0",
  "description": "FinFlux - Personal Finance Tracker",
  "private": true,
  "scripts": {
    "build": "cd webapp && npm install && npm run build && cd ../webapi && npm install",
    "start": "cd webapi && npm start",
    "dev:client": "cd webapp && npm run dev",
    "dev:server": "cd webapi && npm run dev",
    "vercel-build": "cd webapp && npm install && npm run build && mkdir -p ../webapi/build && cp -r dist/* ../webapi/build/ && cd ../webapi && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}